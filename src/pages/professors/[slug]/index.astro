---
import Layout from "../../../layouts/Layout.astro";
import CV from "../../../components/react/CV";
const { slug } = Astro.params;
const token = import.meta.env.ACCESS_TOKEN;
const url = import.meta.env.API_URL;
---

<Layout title="CV">
  <div class="max-w-4xl mx-auto py-12">
    <div
      class="body-animate backdrop-blur-xl rounded-lg overflow-hidden shadow-2xl"
    >
      <CV url={url} token={token} slug={slug} client:only />
    </div>
  </div>
</Layout>

<style>
  .body-animate {
    background: linear-gradient(-45deg, #5e548e, #ffd6ff, #34a0a4, #b5e2fa);
    background-size: 400% 400%;
    animation: gradient 15s ease infinite;
  }
  @keyframes gradient {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
</style>
