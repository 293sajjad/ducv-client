<footer class="py-20 md:py-40 text-center">
  <p class="text-gray-600 fancy-fade-in">&copy; 2024 حق کپی‌رایت محفوظ است.</p>
</footer>

<style>
  @keyframes fancyFadeIn {
    0% {
      opacity: 0;
      transform: scale(0.95);
      color: #d1d5db; /* رنگ اولیه (خاکستری روشن) */
    }
    50% {
      opacity: 0.5;
      transform: scale(1.02);
      color: #6b7280; /* رنگ میانی (خاکستری تیره‌تر) */
    }
    100% {
      opacity: 1;
      transform: scale(1);
      color: #4b5563; /* رنگ نهایی (خاکستری تیره) */
    }
  }
  .fancy-fade-in {
    animation: fancyFadeIn 3s ease-in-out;
  }
</style>
